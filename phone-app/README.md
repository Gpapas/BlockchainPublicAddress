# Phone App (React Native)

Simple React Native example that interacts with the PoC backend.

1. Install dependencies using Expo or React Native CLI.
2. Start the backend so the app can reach `http://localhost:3000`.
3. Run the app on a simulator or device.

The interface lets you verify a phone number via Vonage, then bind a blockchain address to it and retrieve existing bindings.
